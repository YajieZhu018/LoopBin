# --- Example data (downloaded) + pipeline outputs — too large to commit ---
example/example_data/
example/run_output/
example/run_*/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/

# --- uv-managed virtualenv (see CLAUDE.md: `source .venv/bin/activate`) ---
.venv/

# --- Personal/cluster-specific job scripts (paths/account not portable) ---
loopbin/jobs/experiments/

# --- Superseded conda spec, kept locally for reference only (see loopbin.yml) ---
loopbin_core.yml

# --- Pipeline data: 00_raw / 01_preprocess / 02_process outputs ---
data/

# --- SLURM job logs ---
*.out
*.err

# --- Large arrays and model checkpoints ---
*.npy
*.npz
*.h5
*.hdf5
*.pkl
*.pb
*.index
*.data-[0-9]*-of-[0-9]*
checkpoint

# --- Claude Code local session state ---
**/.claude/settings.local.json
.claude_verify/

# --- editor/tool temp artefacts ---
*.new
*.sbatch.tmp.*

# --- macOS ---
.DS_Store
